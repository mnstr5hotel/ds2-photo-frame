# DEATH STRANDING 2 PHOTO SIMULATOR 项目总结

总结日期：2026-08-12

## 1. 最终成果

本项目已从 DS2 拍照模式素材交接发展为可公开使用的纯静态照片合成器，并完成 GitHub 组织迁移、GitHub Pages 发布、移动端适配、素材加载优化和维护文档整理。

- 产品名：`DEATH STRANDING 2 PHOTO SIMULATOR`
- 中文副标题：`死亡搁浅2照片模拟器`
- 正式网站：`https://mnstr5hotel.github.io/ds2-photo-simulator/`
- 仓库：`https://github.com/mnstr5hotel/ds2-photo-simulator`
- 当前产品规模：41 个相框、24 个贴纸、24 种贴纸颜色
- 最终导出：2560 × 1440 PNG

## 2. 关键阶段与决策

### 阶段一：素材接入和界面闭环

- 读取 `DS2网页素材交接-2026-08-10`，将相框、贴纸、中英文名称和颜色表接入网页。
- 以解包素材和运行时配置为依据，不使用近似图标、手绘文字或不正确拉伸的替代实现。
- 移除网页中的重复贴纸 `S04`、`S14`、`S16`，但保留本地原始文件。
- 最终网页素材固定为 41 个相框和 24 个贴纸。

### 阶段二：编辑器交互完善

- 修复调整贴纸尺寸、旋转和层级时左侧素材栏闪动，核心原则是不因编辑状态变化重建素材列表。
- 将取消贴纸选中的范围扩展到画布外的网页区域，同时让工具栏操作保持选中。
- 修复画布中间细竖线和纹理层级；点阵/扫描线只作为页面底层背景，不能覆盖照片、相框或编辑画面。
- 为贴纸加入 24 色换色，默认 `SnowWhite`，颜色名称随语言切换且不显示内部编号。
- 多轮尝试颜色菜单和滚动条视觉效果后，最终回归克制、细线、低荧光的 DS2 风格。

### 阶段三：照片与移动端能力

- 为照片调整模式补齐移动、缩放、旋转、重置和亮度工具栏。
- 亮度范围设为 50%–150%，预览与导出共用同一渲染逻辑。
- 优化竖屏移动端工具栏换行、宽度和颜色选择器左右平衡。
- 固定以 2560 × 1440 PNG 输出；普通常见图片格式不主动有损压缩，超 5120 最长边才降采样。

### 阶段四：部署路线选择

- 首先尝试 OpenAI Sites，但公开访问需要登录，且中国大陆可用性不符合目标。
- 尝试腾讯 EdgeOne Pages，并成功完成一次构建和临时地址部署；免费地址自定义能力有限，带 `eo_token` 的链接也只是临时预览。
- 在不购买域名的前提下，最终选择公开 GitHub 仓库 + GitHub Pages。该方案免费、自动部署、无需用户登录，但接受源码/素材公开和中国大陆网络不稳定的限制。
- 仓库迁移到组织 `mnstr5hotel`，最终重命名为 `ds2-photo-simulator`；Pages 地址随仓库路径同步变化。

### 阶段五：性能优化与发布稳定性

- 将网页运行素材转换为透明 WebP，降低传输体积。
- 增加 1280 边长 preview 层：点击素材时先加载预览，导出时才读取当前素材的高清 optimized 版本。
- 首屏缩略图优先、其余懒加载；悬停/聚焦时预取；避免点击时产生重复 preload 请求。
- 增加 Service Worker 静态缓存，图片缓存优先，代码和 JSON 网络优先。
- 移除 GitHub Pages 线上 smoke job。原因不是功能错误，而是部署完成与 CDN 刷新之间存在时序差异，容易产生噪音邮件；完整 smoke 改为本地或发布后手动执行。

### 阶段六：正式命名

- 中文名最终确定为 `死亡搁浅2照片模拟器`。
- 英文名最终确定为全大写 `DEATH STRANDING 2 PHOTO SIMULATOR`。
- 浏览器标题、左上角标题、语言包、应用名称、Apple 移动端名称、Open Graph、Twitter 分享标题、README 和 GitHub 项目描述均已同步。
- 仓库和 URL 路径保持适合网址的 `ds2-photo-simulator`。

## 3. 主要问题、根因与最终方案

### 黑色相框不可辨认

- 症状：没有照片时，黑色边缘与网页背景混在一起，页面纹理还会穿过相框。
- 根因：预览画布没有独立的不透明底色，纹理层级也过高。
- 最终方案：无照片的编辑预览使用无纹理深灰底；页面纹理只在界面底层。正式导出仍使用纯黑底。

### 素材栏闪动

- 症状：调整贴纸或层级时，左栏缩略图整体刷新并改变观感/滚动位置。
- 根因：编辑状态更新触发素材列表重新渲染。
- 最终方案：素材列表只在分类、语言或目录真正变化时更新，贴纸编辑只刷新画布和工具栏。

### 素材点击后预览慢

- 症状：缩略图可见，但点击后画布仍需长时间等待。
- 根因：最初直接下载和解码高清素材；GitHub Pages 网络延迟进一步放大等待。
- 无效或不足的方案：仅优化缩略图、仅增加 preload、继续依赖高清原图，都无法解决点击后的关键路径。
- 最终方案：preview/optimized 两级素材、选中优先、内存复用、导出时再加载高清。
- 剩余限制：中国大陆访问 GitHub Pages 的底层网络问题不能通过前端完全消除。

### 素材保护与公开部署冲突

- 初始目标：避免用户轻易直接获取素材。
- 尝试方案：服务端素材路由、HMAC、会话 Cookie、EdgeOne/OpenAI Sites 运行层。
- 问题：增加部署复杂度、登录或地区可用性问题，同时公开源码后无法形成真正保护。
- 最终决策：接受公开静态资源，删除运行时加密依赖，把稳定、免费和直接访问放在优先级更高的位置。

### GitHub Actions 噪音失败

- 症状：Smoke 阶段反复失败并发送通知，而网站稍后实际可用。
- 根因：部署完成状态与 GitHub Pages/CDN 新资源可见时间不完全一致。
- 最终方案：Actions 保留确定性的 check/build/deploy；线上 smoke 从自动 workflow 移除，改为发布后手动执行。

### 内置浏览器与外部浏览器表现不同

- 症状：Codex 内置浏览器一度不显示素材，而外部浏览器正常。
- 判断：特定浏览器会话、缓存或安全环境差异，不等同于正式网站故障。
- 处理原则：同时用本地静态服务、Playwright smoke 和正式公网 URL 验证，不凭单一嵌入式浏览器下结论。

## 4. 验证闭环

项目现有三层验证：

1. `npm.cmd run check`：JavaScript 语法、素材数量、名称、路径、SHA-256、语言键和服务端依赖边界。
2. `npm.cmd run build`：生成完整 `dist-pages/`，确认 41 个相框和 24 个贴纸进入静态目录。
3. `npm.cmd run smoke`：验证首页、24 贴纸、41 相框、24 色、F37、上传、换色、素材直连和 PNG 导出。

最近一次正式发布（2026-08-12）已通过 GitHub Actions build/deploy 和线上 smoke。Actions 中官方 Pages Action 的 Node 20 弃用提示是警告，不是本项目构建失败。

## 5. 用户工作方式与维护偏好

- 偏好先在本地页面直接观察，再逐项提出视觉和交互问题。
- 对素材真实性要求高：以截图、解包纹理和运行时配置为准，不接受近似替代。
- 视觉上偏好克制的 DS2 科技界面，不喜欢过强颗粒、过量荧光或明显脱离原风格的装饰。
- 发布方案优先考虑免费、无需登录、任何人可直接访问；在此条件下愿意接受公开源码和素材。
- 对 CI 噪音敏感：确定性检查保留，易受 CDN 时序影响的线上自动检查应避免重复通知。
- 习惯在项目稳定后进入总结/交接模式，把维护工作移交给新的对话。

## 6. 通用经验

- 优化交互延迟应先拆分网络、解码、处理和渲染，不要只看总资源体积。
- 编辑器 UI 状态和素材目录渲染应解耦，避免局部操作导致昂贵列表重建。
- 静态公开部署不应包装成“素材安全”；最多只能降低偶然下载便利性。
- CI 应验证确定性结果，易受最终一致性影响的线上 smoke 更适合部署后重试或人工触发。
- 产品名、仓库 slug、浏览器标题和分享元信息是不同层级；改名时应建立同步清单。

## 7. 后续优先事项

1. 若目标用户集中在中国大陆，购买独立域名并迁移/镜像至 EdgeOne、COS + CDN 或 OSS + CDN。
2. 若继续优化点击延迟，先采集真实 Network/Performance 数据，再决定是否降低 preview 分辨率、使用 AVIF 或做预取分组。
3. 为移动端手势、照片亮度和 2560 × 1440 输出尺寸增加更细的自动化断言。
4. 关注 GitHub Pages 官方 Action 的 Node 运行时升级，适时更新 workflow 版本。
5. 新增素材时严格执行索引、三层 WebP、中英文名称和 SHA-256 的完整链路。
