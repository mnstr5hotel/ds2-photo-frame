# Frontend Baseline

本文件记录当前必须保持的前端行为边界。它取代早期原型阶段的画布自适应、透明导出和锁定贴纸方案。

## 项目边界

- 当前目录只负责网页照片合成，不处理 Decima 解包、素材提取或自动筛选。
- 浏览器编辑核心是原生 HTML/CSS/JavaScript；发布时由构建脚本生成纯静态目录，并通过 GitHub Pages 提供公开访问。
- 所有用户照片只在浏览器本地解码、调整和合成，不上传服务器，不写入持久化存储。

## 固定合成合同

- 逻辑画布和导出尺寸固定为 `2560 x 1440`。
- 导出格式固定为 PNG，底色固定为黑色。
- 无照片时，编辑预览使用无纹理深灰底以辨认黑色相框；该预览底色不进入导出，导出仍为纯黑底。
- 层级从下到上：黑色背景、照片、相框下贴纸、固定相框、相框上贴纸。
- 每次合成只允许一张照片、一个相框和最多两个贴纸。
- 相框固定铺满画面，不可移动、缩放或旋转；新相框自动替换旧相框。
- 贴纸默认位于相框下方，可逐个切换到相框上方。
- 每个贴纸独立保存一个调色板颜色；新贴纸固定默认为 `SnowWhite`。

## 编辑合同

- 照片首次上传和更换后按 Cover 居中铺满。
- 照片可移动、按 90 度旋转、缩放至基础比例的 10%–400%，允许露出黑边。
- 照片支持 X/Y 中心轴与四边吸附；贴纸支持 X/Y 中心轴和直角旋转吸附。
- 照片和贴纸均显示编辑示廓框，并支持四角等比缩放；贴纸另有旋转控制点。
- 点击画布和编辑工具条以外的网页区域时，取消当前贴纸选中框；编辑工具条操作本身保持选中。
- 贴纸编辑工具栏包含 24 色选择，选项按界面语言读取官方 `names.en` 或 `names['zh-Hans']`，不显示序号或颜色编码。
- 贴纸、相框、已选素材名称同样按界面语言读取素材索引中的官方名称；语言切换必须即时更新显示。
- 换色使用 `out.rgb = source.rgb * tint.rgb / 255` 并保留源 alpha；颜色变化必须进入撤销/重做和最终导出。
- 桌面支持拖动、滚轮缩放；移动端支持单指拖动和照片双指缩放。
- 照片调整是开关模式。开启时隐藏相框和贴纸、禁用素材入口和下载，关闭后恢复。
- 撤销/重做最多 30 步，连续拖动、滚轮和双指手势合并为单步；照片模式有独立历史边界。

## UI 合同

- 保持当前《死亡搁浅 2》菜单风格：纯黑底、冰蓝细线、点阵、低透明度全息填充与克制动效。
- 左上标题固定为英文 `DEATH STRANDING 2 PHOTO SIMULATOR`，不随语言切换。
- 素材分类仅有“贴纸”和“相框”；照片调整开关固定在左侧素材栏底部。
- 编辑工具条位于画布外下方，不得遮挡合成画面；撤销/重做按钮位于工具条旁。
- 撤销/重做文字提示显示 5 秒并严格位于底部状态栏中央。
- 四张随机背景图只装饰左侧素材栏，不得出现在画布或导出结果中。
- 调整贴纸大小、旋转或相框上下层级时不得重建左侧素材列表，避免缩略图闪动和滚动位置变化。
- 全页质感层保留低亮度扫描线和点阵，但它只能作为页面底层背景；所有面板、文字、按钮、检视区、照片和相框均位于纹理层上方。
- 检视区不显示常驻的装饰性中心轴；吸附参考线仅在拖动并触发吸附时显示。
- 保留中英文切换、非官方非商业免责声明和 `prefers-reduced-motion` 支持。

## 输入与部署合同

- 支持 JPEG、PNG、WebP、HEIC 和 HEIF。
- 工作图像最长边限制为 5120px，超出时在浏览器本地降采样。
- HEIC/HEIF 通过 `site/vendor/heic-to-csp-1.5.2.js` 本地转换；完整功能要求 HTTP/HTTPS 静态服务，不支持直接以 `file://` 作为正式入口。
- 页面使用严格 CSP，不依赖运行时 CDN、数据库、上传服务或只读后端；发布素材是可直接访问的公开静态文件。
- 用户有未导出的合成变化时，刷新或关闭页面触发浏览器离开提醒。

## 当前已知状态

- 正式素材库原文件保留 41 张相框和 27 张贴纸；网页索引展示 41 张相框和 24 张去重贴纸。
- `S04`、`S14`、`S16` 只从网页索引排除，本地原图和缩略图不得删除。
- 素材面板使用 WebP 缩略图；选中时先加载 preview WebP，导出时再按需加载 optimized WebP，不得改回全量高清预加载。
- F11–F20、F23、F24 保留 `temporary-v1-manual-promotion` 来源标记，但当前正常展示。
- 页面没有工程保存、自动恢复或云端同步功能。
- 正式基线位于 `main`；维护时仍需先检查工作树，不得用 reset 或 checkout 覆盖用户的未提交修改。

## 回归验收

1. 使用 HTTP 打开页面，确认 24 张贴纸、41 张相框及随机背景图均能加载，且背景图不覆盖画布。
2. 上传横图、竖图、大图和 HEIC，确认本地处理与 5120px 降采样。
3. 测试照片和贴纸移动、角端缩放、按钮/滚轮缩放、旋转与吸附。
4. 确认第三个贴纸被阻止，新相框自动替换旧相框。
5. 确认照片调整模式隐藏其他素材并禁用下载，退出后完整恢复。
6. 检查撤销/重做、5 秒居中提示和未导出离开提醒。
7. 导出 PNG，确认尺寸为 `2560 x 1440`、黑底且没有选框、引导线或源照片元数据。
8. 在 `1440 x 900` 和真实 `390 x 844` 移动视口检查无横向溢出或控件遮挡。
